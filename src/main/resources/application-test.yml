spring:
  datasource:
    url: jdbc:p6spy:mysql://my-frree-db.clwi66oimt5r.ap-northeast-2.rds.amazonaws.com:3306/stock_db?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: rootuser
    driver-class-name: com.p6spy.engine.spy.P6SpyDriver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate.SQL: off             # Hibernate SQL 로그 끄기
    org.hibernate.type.descriptor.sql: off
    org.hibernate.orm.jdbc.bind: off   # 바인딩 로그 끄기
